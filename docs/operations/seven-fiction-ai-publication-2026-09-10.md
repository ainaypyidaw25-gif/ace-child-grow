# Exact seven-fiction AI publication

This release is separate from the completed seven-story correction (PR #231). It does not represent clinician, native Myanmar-language editor, or other human review. It does not approve the remaining clinical catalogue. Publication must not be claimed until activation and independent parent-route read-back are complete.

## Scope

Only revision 3 of these corrected fictional stories is eligible:

- `st_little_seed`
- `st_ba_ba_sounds`
- `st_when_i_feel_angry`
- `st_taking_turns`
- `st_goodnight_moon_friend`
- `st_visit_to_doctor`
- `st_sharing_mango`

The existing legacy allowlist and maximum of three are unchanged. A separate, compiled schema-2 registry supports this exact seven-story set and its multiple source references. Runtime visibility is bounded to the legacy three plus these seven; an unknown or duplicate active target fails closed. The global emergency disable remains applicable. No arbitrary database artifact hash or newly entered slug authorizes publication.

## Evidence and limitations

The actual source-research and independently performed bilingual semantic checks are recorded in private `artifacts/story-release-20260910/` reports. The compiler must pin these original file hashes, the post-correction snapshot and the exact reviewed content hashes. Preserve every linked reference in order, with a separate evidence audit run per story-source relationship.

Publisher pages, abstracts and official search-index copies were accessible to differing degrees. The language textbook and Bright Futures textbook were not reviewed in full; relevant publisher metadata was checked. The NHS language page's 2026 video date is not a new page edition. AAP's May 2026 reaffirmation is not a new toxic-stress article edition. These references support limited educational background, not efficacy or universal child-response claims for original fiction. Do not convert these limitations into a human approval.

Two exact bundled illustrations were visually checked and their production bytes matched; see the correction runbook for asset hashes and the limited historical generation/Owner record. Five stories have no bundled illustration. All 21 database media records are placeholders. Runtime media hashes must fail closed if unreviewed media is introduced. No audio/PDF availability or independent legal rights clearance is claimed.

Parent list and detail views must retain the English/Myanmar AI-only badge and full disclosure. Each body separately identifies original fiction. The ordinary human-review fields and append-only decisions are neither fabricated nor consumed by this AI lane.

## Release gates

1. Compile immutable artifact and full-state preimages from the exact reviewed production snapshot. Reject a mismatched report, content hash, source set, media set or assigned protected batch.
2. Run focused state-machine, multi-source visibility, legacy compatibility, parent rendering and offline withdrawal tests; run full typecheck, lint, unit tests and build. Inspect the actual generated manifest independently.
3. Merge through normal CI. Deploy the exact backend commit without a forced gate bypass. Confirm deployment separately from data activation.
4. Run read-only internal preflight with a fresh explicit observation time and exact compiled identities. All seven must remain dark; existing three, source records, human history, configuration generation and expiry schedules must match the baseline.
5. Stage only actual AI audit records. Staging must not expose content or change the corrected copy, revision, evidence link, source metadata, human history or media.
6. Independently verify staged audit hashes, per-source runs and seven dark targets. Only then activate the exact seven, with a bounded scheduled expiry and unchanged existing three.
7. Independently capture production again. Check expected write set, all ten exact AI parent-readable targets, unchanged protected data and the scheduled cutoff. Re-read parent-facing detail pages, both-language disclosure, citations and mapped illustrations. Record any session/entitlement limits explicitly.

## Withdrawal

An exact release-specific withdrawal may revoke only this release's rows and clear only pointers still matching this release. It must not erase newer successor pointers or human decisions. Scheduled expiry performs a bounded database write so reactive queries invalidate; runtime date checks also reject expired audits. Global emergency disable takes all AI previews offline without changing the human publication lane.

Offline copies are removed after a complete live publication manifest arrives on reconnect. A disconnected device can retain an earlier download until reconnection; do not promise remote erasure from disconnected devices. The detail view must not prefer an old cached copy when a live restricted response is available.

This runbook describes required gates, not a completion receipt. Store observed activation timestamps, hashes, counts, CI links and independent read-back in the separate completion receipt after they actually occur.
